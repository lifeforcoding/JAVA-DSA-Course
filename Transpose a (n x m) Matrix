public static void transposeMatrix(int[][] matrix, int[][] b) {

        for (int i = 0; i < matrix[0].length; i++) {
            for (int j = 0; j < matrix.length; j++)
                b[i][j] = matrix[j][i];
        }
    }
